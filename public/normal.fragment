precision highp float;

// uniform mat4 worldView;

varying vec3 vNormal;

void main(void){
    //view space normal
    vec4 VS_normal = vec4(vNormal, 1);
    VS_normal.z = -VS_normal.z;
    //Normal matrix: transpose(inverse(worldView)) * normal; 
    gl_FragColor = VS_normal;
}
